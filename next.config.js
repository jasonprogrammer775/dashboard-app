/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      'openweathermap.org',
      'media2.dev.to',
      'coin-images.coingecko.com'
    ],
  },
};

module.exports = nextConfig;
